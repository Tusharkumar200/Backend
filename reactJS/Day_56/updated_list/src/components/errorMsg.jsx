const errorMsg =(errItems)=>{
  // let foodItem = ['Pizza', 'Burger', 'Sandwich', 'Pasta', 'Biryani', 'Noodles'];
  return (
    <>
      errItems.length === 0 ? <li className="list-group-item">No food available</li>foodItem;

    </>
    
  )
};

export default errorMsg;